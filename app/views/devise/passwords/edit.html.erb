<div class="container">
  <div class="row">
    <div class="col-xs-4"></div>
    <div class="col-xs-4">
      <h2>Cambiar contraseña</h2>

      <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
        <%= custom_devise_error_messages! %>
        <%= f.hidden_field :reset_password_token %>

        <div><%= f.label :password, "Nueva contraseña" %><br />
        <%= f.password_field :password, :autofocus => true, :class => "form-control" %></div>

        <div><%= f.label :password_confirmation, "Confirma la nueva contraseña" %><br />
        <%= f.password_field :password_confirmation, :class => "form-control" %></div>
        <br/>
        <div><%= f.submit "Cambiar mi contraseña", :class => "btn btn-lg btn-primary btn-block" %></div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
    <div class="col-xs-4"></div>
  </div>
</div>
