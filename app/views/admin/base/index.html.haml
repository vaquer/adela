.container
  - if can? :create, User
    %h2 IMPORTAR USUARIOS
    = form_tag(admin_create_users_path, :method => :post, multipart: true) do |f|
      = label_tag "Archivo CSV"
      %br/
      %small
        Asegúrate de que contenga las columnas:
        %strong "nombre", "email", "organizacion"
      = file_field_tag :csv_file
      %br/
      = submit_tag "Importar usuarios", :class => "btn button"
    %hr/
  - if can? :read, User
    %h2 USUARIOS REGISTRADOS
    = link_to "Ver usuarios registrados", admin_users_path, :class => "btn button"
    %hr/
  - if can? :read, Organization
    %h2 ORGANIZACIONES
    = link_to "Ver organizaciones", admin_organizations_path, :class => "btn button"
