<div class="container">
  <div class="row">
    <div class="col-xs-4"></div>
    <div class="col-xs-4">
      <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => "form-signin"}) do |f| %>
        <h2 class="form-signing-heading">Iniciar sesión</h2>
        <%= f.email_field :email, :autofocus => true, :class => "form-control", :placeholder => "Correo electrónico" %>
        <br/>
        <%= f.password_field :password, :class => "form-control", :placeholder => "Contraseña" %>
        <% if devise_mapping.rememberable? -%>
          <div><%= f.check_box :remember_me %> <%= f.label :remember_me, "No cerrar sesión" %></div>
        <% end -%>

        <div><%= f.submit "Entrar", :class => "btn btn-lg btn-primary btn-block" %></div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
    <div class="col-xs-4"></div>
  </div>
</div>
