<% flash.each do |key, value| %>
  <%= content_tag(:div, content_tag(:span, "", class: "#{bootstrap_flash_class(key)}") + value.html_safe, class: "alert alert-#{bootstrap_flash_class(key)}") %>
<% end %>