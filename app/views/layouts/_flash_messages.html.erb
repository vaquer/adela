 <% unless flash.empty? %>
  <script type="text/javascript">
      <% flash.each do |f| %>
      <% type = f[0].to_s.gsub('alert', 'error').gsub('notice', 'success') %>
     	 toastr['<%= type %>']('<%= f[1] %>');
      <% end %>
  </script>
 <% end %>
 <!-- - flash.each do |key, value|
   = content_tag(:div, content_tag(:span, "", class: "#{bootstrap_flash_class(key)}") + value.html_safe, class: "alert alert-#{bootstrap_flash_class(key)}") -->
