.container
  .row
    .col-xs-4
    .col-xs-4
      = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :class => "form-signin" }) do |f|
        %h2.form-signing-heading ¿Olvidaste tu contraseña?
        = custom_devise_error_messages!
        = f.email_field :email, :autofocus => true, :class => "form-control", :placeholder => "Correo electrónico"
        %br/
        %div= f.submit "Enviar", :class => "btn btn-lg btn-primary btn-block"
      = render "devise/shared/links"
    .col-xs-4
